# git-sync-manager

Deterministic Git sync for distributed vault state.

## Scope
- Enforces Platinum cloud/local boundaries
- Writes only to allowed vault paths
- Leaves final execution to Local when required
